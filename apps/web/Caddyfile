{
	admin off
	auto_https off
}

:80 {
	@api path /health /scenes /song/* /sounds /stats /user/* /api/* /swagger* /static/*
	handle @api {
		reverse_proxy api:3000
	}
	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}